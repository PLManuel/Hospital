server:
  port: 8096

spring:
  application:
    name: analysis-cart
  datasource:
    url: jdbc:h2:mem:analysisCartdb
    driver-class-name: org.h2.Driver
    username: sa
    password:
  h2:
    console:
      enabled: true
      path: /h2-console
  jpa:
    show-sql: true
    open-in-view: false
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
